## 目标
- 在 `src` 中补齐 5174 端口缺失的功能与样式，使其与 3456（dist）一致，并确保后续 `npm run build` 打包与预览一致。

## 变更摘要
- 新增“分数筛选”支持 4 分以上与全部新闻：调整 `ReportGenerator.jsx` 查询参数构造与 UI 控件。
- 新增“多 prompt 选择”：后端提供 `GET /api/keyword-prompts`，前端按关键词加载候选 prompt 并在生成时携带 `promptId`。
- 新增“短总结选择”：UI 开关传递 `summaryVersion='short'` 至生成接口，后端已支持（`server.cjs:806-872`）。
- 导航与页面图标优化：为“字数统计”等导航项补充图标，保证设计一致性（`src/components/Layout.jsx:11-18`）。
- 可选：开启构建 source map，避免将来无法定位问题（`vite.config.js`）。

## 后端改动
- `server.cjs`
  - 添加 `GET /api/keyword-prompts?keyword=...`：读取 `config/keyword-prompts.json`，返回指定关键词的 `{id,name,description}` 列表，供前端展示与选择。
  - 现有 `POST /api/generate-report` 已支持 `promptId` 与 `summaryVersion`（`server.cjs:806-872`、`server.cjs:823-843`），无需变更核心逻辑，只需保证前端传参。

## 前端改动
- `src/pages/ReportGenerator.jsx`
  - 状态与UI：
    - 添加 `scoreFilter`（值：`all|min3|min4`），对应构造查询：
      - `all` → `includeAll=true`
      - `min3` → `minScore=3`
      - `min4` → `minScore=4`
      - 变更点：`handleSearchNews` 参数拼接（现行为 `includeAll`/`minScore=3`，见 `src/pages/ReportGenerator.jsx:163-171`）。
    - 添加 `summaryVersion`（值：`full|short`），UI 切换开关，传给生成接口。
    - 添加 `promptOptions` 与 `selectedPromptId`，在关键词变化时请求 `/api/keyword-prompts` 并渲染下拉选择。
  - 生成接口调用：
    - 在 `handleGenerateReport` 与 `handleGenerateKimiReport` 里追加 `promptId` 和 `summaryVersion` 字段（`src/pages/ReportGenerator.jsx:696-703`、`src/pages/ReportGenerator.jsx:878-885`）。
- `src/components/Layout.jsx`
  - 为导航项添加图标（例如使用 `lucide-react`），最少为“字数统计”添加图标（`src/components/Layout.jsx:17`）。

## 构建一致性
- 验证流程：
  - 开发验证：`npm run dev`，在 `http://localhost:5174` 验证筛选、prompt 选择、短总结与图标等。
  - 打包与预览：`npm run build` 后，使用 `npm run preview` 或访问 `http://localhost:3456` 验证构建页面与 5174 一致。
- 可选增强：在 `vite.config.js` 增加 `build: { sourcemap: true }`，保留 source map 以便定位问题。

## 风险与回退
- 如果 `config/keyword-prompts.json` 某关键词缺失，将前端 UI 优雅降级为“默认 prompt”。
- 若后端读取配置失败，前端自动隐藏“多 prompt 选择”，提示使用默认配置。

## 交付
- 完成上述改动并自测全链路；你在 5174 端口进行验收后，我再进行构建预览开箱确认，确保不再出现版本不一致。