## 功能实现计划：显示完整提示词

为了让您能够检查发送给 LLM 的完整提示词，我将进行以下修改：

### 1. 后端修改 (`server.cjs`)
*   **API 增强**：修改 `/api/policy/extract` 和 `/api/policy/compare` 接口。
*   **新增返回字段**：在返回的 JSON 数据中增加 `debug` 字段，包含完整的 `finalUserPrompt`（最终发送给 LLM 的用户提示词）和 `systemPrompt`。

### 2. 前端修改 (`WeeklyComparison.jsx`)
*   **状态管理**：增加状态变量来存储后端返回的 Prompt 信息。
*   **UI 展示**：
    *   在 **Step 2 (提取政策)** 的结果预览页面，增加一个可展开的 "查看完整提示词" 区域，显示提取阶段使用的 Prompt。
    *   在 **Step 3 (对比分析)** 完成后的结果页面，同样增加一个 "查看完整提示词" 区域，显示对比阶段使用的 Prompt。
    *   提示词内容将使用文本框或代码块展示，方便复制和检查。

### 3. 预期效果
*   当您点击"下一步：提取政策"后，在看到提取结果的同时，可以点击查看到底发给了 AI 什么内容（包含了替换后的模板和周报内容）。
*   当您点击"开始对比分析"后，在看到对比报告的同时，也可以查看发送给 AI 的对比指令。

请确认是否执行此计划？